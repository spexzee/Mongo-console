# App Metadata Storage (Stores saved connections, query history, saved queries, and backup records)
APP_MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/?retryWrites=true&w=majority

# 32-character secret key for encrypting connection credentials at rest
APP_ENCRYPTION_KEY=your_32_character_encryption_key_here
